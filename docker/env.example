#Services ports

# The apache port
HTTP_SERVER_PORT=8080

# The PHP version
PHP_VERSION=7.4

# The X_DEBUG port (The host port which the php client will be connected to the xdebug server)
X_DEBUG_PORT=9000
